//
// Created by Dru on 9/5/2024.
//

/*#include "Test.h"
#include <iostream>


void Test::runTests() {
    test<Letter>(Letter('Y'), "Letter: getLetter():", [](const Letter& letter) {
        return letter.getLetter() == 'Y';
    });
}

template <typename T>
void Test::test(const T& object, const std::string& testName, bool lambda(const T& object)) {
    std::cout << "Testing " << testName << ": ";
    if (lambda(object))
        std::cout << "PASSED\n";
    else
        std::cout << "FAILED\n";
}
*/